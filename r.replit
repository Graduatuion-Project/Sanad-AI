modules = ["web", "python-3.12"]
run = "uvicorn main:app --host 0.0.0.0 --port 3000"

[nix]
channel = "stable-24_05" # You can keep your current channel
packages = [
  "cacert",
  "glibcLocales",
  "libGL",
  "libGLU",
  "libxcrypt",
  "openjdk17_headless", # <--- ADD THIS LINE
]

[deployment]
run = ["sh", "-c", "uvicorn main:app --host 0.0.0.0 --port 3000"]

[[ports]]
localPort = 3000
externalPort = 80